{
  "name": "@aloud/app",
  "version": "0.1.0",
  "private": true,
  "description": "Aloud — accessible cross-platform read-aloud reader (RN/TypeScript layer).",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.ci.json --noEmit",
    "lint": "eslint \"src/**/*.{ts,tsx}\""
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "typescript": "^5.5.4",
    "vitest": "^2.1.9"
  },
  "//": "react/react-native are peer deps of the app shell; the unit-tested modules (ViewModel, a11y, contract, trace) intentionally do NOT import them, so the test suite runs in plain Node."
}
